<h1>Burger Time!</h1>

<h2>Burgers - get 'em while they're hot!</h2>

<ul>
  {{#each this}}
   {{> burger/burger-block}}
  {{/each}}
</ul>

<h2>Yum! These burgers were delicious!</h2>

<ul>
  {{#each cats}}
    {{#if sleepy}}
      {{> cats/cat-block sleep=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Serve up a new burger!</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Burger Name:</label>
    <input type="text" id="ca" name="name">
  </div>

  <div class="form-group">
    <label for="eat">Eaten?</label><br>
    <input type="radio" name="eat" value="1" checked> Served!<br>
    <input type="radio" name="eat" value="0"> Eaten!
  </div>

  <button type="submit">Add Burger</button>
</form>